#include <iostream>
#include <vector>

using namespace std;

int n, k;
vector<int> numbers;

int main(void)
{
    cin >> n >> k;

    for (int i = 0; i < n; i++)
    {
        int tmp;
        cin >> tmp;
        numbers.push_back(tmp);
    }

    
}